version: '3'

networks:
  bau:

services:
  web:
    container_name: ${APP_NAME}_web
    build:
      context: ./docker/web
    ports:
      - "80:80"
    volumes:
#      Root path
      - ./src/demo1:/var/www/demo1
      - ./src/demo2:/var/www/demo2
#      Config vhost
      - ./docker/web/demo1.conf:/etc/nginx/conf.d/demo1.conf
      - ./docker/web/demo2.conf:/etc/nginx/conf.d/demo2.conf
#      Config PHP-FPM
      - ./docker/web/www.conf:/etc/php-fpm.d/www.conf
#      Config PHP
      - ./docker/web/php.ini:/etc/php.ini
#      Logs
      - ./docker/web/logs:/var/log/nginx
    networks:
      bau:
  mysql:
    container_name: ${APP_NAME}_mysql
    image: mysql:8.0
    ports:
      - "${MYSQL_PORT}:3306"
    volumes:
      - ./docker/mysql:/var/lib/mysql
    command: --sql_mode=""
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    networks:
      bau:
  redis:
    container_name: ${APP_NAME}_redis
    build:
      context: ./docker/redis
    ports:
      - "${REDIS_PORT}:6379"
